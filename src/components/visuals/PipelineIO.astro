---
// 2.1 — Think big: pipeline I/O before/after
// Uses RustQC as a real example (https://seqeralabs.github.io/RustQC)
---

<div class="pipeline-wrap">
  <div class="pipeline-scenario">
    <div class="scenario-label scenario-before">Before — 15+ separate tools, each reading the BAM independently</div>
    <div class="pipeline-row">
      <div class="tool-box">
        <div class="tool-name">featureCounts</div>
        <div class="tool-sub">quantify</div>
      </div>
      <div class="io-sep">
        <div class="disk-icon">💾</div>
      </div>
      <div class="tool-box">
        <div class="tool-name">dupRadar</div>
        <div class="tool-sub">duplication</div>
      </div>
      <div class="io-sep">
        <div class="disk-icon">💾</div>
      </div>
      <div class="tool-box">
        <div class="tool-name">RSeQC</div>
        <div class="tool-sub">QC metrics</div>
      </div>
      <div class="io-sep">
        <div class="disk-icon">💾</div>
      </div>
      <div class="tool-box tool-box-etc">
        <div class="tool-name">+12 more…</div>
        <div class="tool-sub">each reads BAM</div>
      </div>
    </div>
    <div class="cost-row">
      <span class="cost-item cost-bad">15+ BAM reads</span>
      <span class="cost-item cost-bad">~2h 05m total</span>
    </div>
  </div>

  <div class="pipeline-divider">↓ rethink the architecture</div>

  <div class="pipeline-scenario">
    <div class="scenario-label scenario-after">After — RustQC: single pass over the BAM</div>
    <div class="pipeline-row pipeline-row-after">
      <div class="tool-box tool-box-bam">
        <div class="tool-name">sample.bam</div>
        <div class="tool-sub">read once</div>
      </div>
      <div class="io-arrow-clean">
        <div class="arrow-line"></div>
      </div>
      <div class="tool-box tool-box-combined">
        <div class="tool-name">RustQC</div>
        <div class="tool-sub">all metrics, one pass</div>
        <div class="tool-badge">~5 min</div>
      </div>
      <div class="io-arrow-clean">
        <div class="arrow-line"></div>
      </div>
      <div class="tool-box">
        <div class="tool-name">MultiQC</div>
        <div class="tool-sub">report</div>
      </div>
    </div>
    <div class="cost-row">
      <span class="cost-item cost-good">1× BAM read</span>
      <span class="cost-item cost-good">~25× faster</span>
      <span class="cost-item cost-good">820,118 values, zero mismatches</span>
    </div>
  </div>

  <div class="pipeline-source">
    Source: <a href="https://seqeralabs.github.io/RustQC" target="_blank" rel="noopener">seqeralabs.github.io/RustQC</a>
  </div>
</div>

<style>
  .pipeline-wrap {
    background: var(--bg-secondary);
    border-left: 3px solid var(--section-color, var(--accent-rust));
    border-top: none;
    border-right: none;
    border-bottom: none;
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .pipeline-scenario {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .scenario-label {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.7rem;
    font-weight: 600;
    margin-bottom: 4px;
    line-height: 1.5;
  }

  .scenario-before { color: rgba(212, 86, 90, 0.7); }
  .scenario-after  { color: rgba(93, 172, 114, 0.7); }

  .pipeline-row {
    display: flex;
    align-items: center;
    gap: 4px;
    flex-wrap: wrap;
    row-gap: var(--space-sm);
  }

  .tool-box {
    background: var(--bg-primary);
    border: 1px solid rgba(230, 225, 216, 0.1);
    border-radius: 6px;
    padding: var(--space-xs) var(--space-sm);
    text-align: center;
    flex-shrink: 0;
  }

  .tool-box-etc {
    opacity: 0.5;
  }

  .tool-box-bam {
    opacity: 0.7;
  }

  .tool-box-combined {
    border-color: rgba(93, 172, 114, 0.3);
    background: rgba(93, 172, 114, 0.04);
    min-width: 140px;
  }

  .tool-name {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .tool-sub {
    font-family: var(--font-sans);
    font-size: 0.65rem;
    color: var(--text-tertiary);
    margin-top: 2px;
  }

  .tool-badge {
    font-family: var(--font-sans);
    font-size: 0.62rem;
    font-weight: 600;
    color: rgba(93, 172, 114, 0.8);
    background: rgba(93, 172, 114, 0.1);
    border: 1px solid rgba(93, 172, 114, 0.2);
    border-radius: 3px;
    padding: 1px 6px;
    margin-top: 4px;
    display: inline-block;
  }

  .io-sep {
    font-size: 0.85rem;
    color: rgba(212, 86, 90, 0.5);
    padding: 0 2px;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .disk-icon {
    font-size: 0.85rem;
    line-height: 1;
  }

  .io-arrow-clean {
    display: flex;
    align-items: center;
    flex: 1;
    min-width: 24px;
  }

  .arrow-line {
    height: 2px;
    background: rgba(93, 172, 114, 0.3);
    width: 100%;
    position: relative;
  }

  .arrow-line::after {
    content: '';
    position: absolute;
    right: -1px;
    top: -4px;
    width: 0;
    height: 0;
    border-left: 6px solid rgba(93, 172, 114, 0.4);
    border-top: 5px solid transparent;
    border-bottom: 5px solid transparent;
  }

  .cost-row {
    display: flex;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .cost-item {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 600;
    padding: 2px 8px;
    border-radius: 3px;
  }

  .cost-bad {
    background: rgba(212, 86, 90, 0.1);
    color: rgba(212, 86, 90, 0.8);
    border: 1px solid rgba(212, 86, 90, 0.2);
  }

  .cost-good {
    background: rgba(93, 172, 114, 0.08);
    color: rgba(93, 172, 114, 0.8);
    border: 1px solid rgba(93, 172, 114, 0.2);
  }

  .pipeline-divider {
    text-align: center;
    font-family: var(--font-sans);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    padding: var(--space-xs) 0;
    border-top: 1px solid rgba(230, 225, 216, 0.06);
    border-bottom: 1px solid rgba(230, 225, 216, 0.06);
  }

  .pipeline-source {
    font-family: var(--font-serif);
    font-size: 0.7rem;
    color: var(--text-tertiary);
    border-top: 1px solid rgba(230, 225, 216, 0.06);
    padding-top: var(--space-sm);
  }

  .pipeline-source a {
    color: var(--color-link);
    font-family: var(--font-mono);
  }
</style>
