---
// 2.2 — Work small: iteration loop diagram
---

<div class="loop-wrap">
  <div class="loop-label">The Validation Loop</div>
  <div class="loop-diagram">
    <div class="loop-row-main">
      <div class="loop-step">
        <div class="step-num">01</div>
        <div class="step-text">Pick one<br />function</div>
      </div>
      <div class="loop-arrow">→</div>
      <div class="loop-step">
        <div class="step-num">02</div>
        <div class="step-text">Write<br />rewrite</div>
      </div>
      <div class="loop-arrow">→</div>
      <div class="loop-step">
        <div class="step-num">03</div>
        <div class="step-text">Compare<br />outputs</div>
      </div>
    </div>
    <div class="loop-row-outcomes">
      <div class="loop-outcome loop-outcome-fail">
        <span class="outcome-icon">✗</span>
        <span class="outcome-text">Mismatch? Fix &amp; repeat step 2</span>
      </div>
      <div class="loop-outcome loop-outcome-pass">
        <span class="outcome-icon">✓</span>
        <span class="outcome-text">Match? Move to next function</span>
      </div>
    </div>
  </div>
  <div class="loop-note">
    Never move on until the current step validates. Compound errors will bury you.
  </div>
</div>

<style>
  .loop-wrap {
    background: var(--bg-secondary);
    border-left: 3px solid var(--section-color, var(--accent-rust));
    border-top: none;
    border-right: none;
    border-bottom: none;
    padding: var(--space-lg);
  }

  .loop-label {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--text-tertiary);
    margin-bottom: var(--space-md);
  }

  .loop-diagram {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    margin-bottom: var(--space-md);
  }

  .loop-row-main {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
  }

  .loop-step {
    background: var(--bg-primary);
    border: 1px solid rgba(230, 225, 216, 0.1);
    border-radius: 6px;
    padding: var(--space-sm) var(--space-md);
    text-align: center;
    flex: 1;
  }

  .step-num {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--accent-rust);
    margin-bottom: 4px;
  }

  .step-text {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    color: var(--text-secondary);
    line-height: 1.4;
  }

  .loop-arrow {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--text-tertiary);
    flex-shrink: 0;
  }

  .loop-row-outcomes {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-sm);
  }

  @media (max-width: 560px) {
    .loop-row-outcomes {
      grid-template-columns: 1fr;
    }
  }

  .loop-outcome {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    border-radius: 4px;
    font-family: var(--font-sans);
    font-size: 0.7rem;
  }

  .loop-outcome-fail {
    background: rgba(212, 86, 90, 0.08);
    border: 1px solid rgba(212, 86, 90, 0.2);
  }

  .loop-outcome-fail .outcome-icon {
    color: rgba(212, 86, 90, 0.9);
  }

  .loop-outcome-pass {
    background: rgba(93, 172, 114, 0.08);
    border: 1px solid rgba(93, 172, 114, 0.2);
  }

  .loop-outcome-pass .outcome-icon {
    color: rgba(93, 172, 114, 0.9);
  }

  .outcome-icon {
    font-weight: 700;
    flex-shrink: 0;
  }

  .outcome-text {
    color: var(--text-secondary);
  }

  .loop-note {
    font-family: var(--font-serif);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    border-top: 1px solid rgba(230, 225, 216, 0.06);
    padding-top: var(--space-sm);
    font-style: italic;
  }
</style>
